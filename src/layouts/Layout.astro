---
interface Props {
  title: string;
}

const { title } = Astro.props;
import favicon from '../../public/favicon.svg?url';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Lily Ballif's portfolio" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href={favicon} />
    <title>{title}</title>
  </head>
  <body class="h-[100vh] flex flex-col first-letter justify-between">
    <slot />
  </body>
</html>